# 结论：关闭这条政策，保留预测器；下一项只做 NBS V2

这次结果已经足够明确：

```text
510300_PRICE_PATH_DSV5_RISK_BUDGET_POLICY_V1
= ARCHIVED_NO_VIEW_INSUFFICIENT_POLICY_VARIATION

B1_DSV5_PRICE_RISK_FORECAST
= RETAIN_AS_FORECAST_BENCHMARK_ONLY

B1_POSITION_MAPPING
= DISABLED

HISTORICAL_POLICY_REDESIGN
= NOT_ALLOWED

CURRENT_VALIDATED_HIGH_SHARPE_STRATEGY
= NONE

POSITION_IMPACT
= 0
```

不能把四档边界降低、改成连续仓位、改用波动率目标或加入慢恢复机制。当前项目治理明确禁止已观察结果后通过改变窗口、阈值、方向、标签、权重或制度阶段救援。

同时，**不要把B1本身废掉**。它在预测层确实优于B0，只是这种预测差异太小，无法支持当前仓位政策。它现在应当成为后续风险模型必须击败的价格基准，而不是交易策略。

---

# 一、为什么没有必要继续每天运行原DSV5政策任务

历史上：

```text
非100%目标原点 = 2 / 178 = 1.12%
```

按每5个交易日一个原点估算，每年约52个原点，对应：

```text
预期非100%原点约 0.58个/年
```

从当前2个增加到12个，在相同历史频率下大约还需要：

$$
\frac{10}{0.58}\approx17\text{年}
$$

这只是机械外推，不是未来预测，但足以说明：

> 继续把该政策作为每日主动任务，研究效率极低。

## 任务调整

原任务：

```text
dsv5-g1
```

继续保持：

```text
PAUSED
```

不要在工作日19:30重新打开原“政策前向记录”。

可以另建一个只读任务：

```text
510300_B1_DSV5_FORECAST_OBSERVATORY_V1
```

运行规则：

```text
工作日19:30触发
非固定五交易日原点：
    NOT_ORIGIN_NOOP

固定原点日：
    只记录B0/B1预测，不产生目标仓位
```

记录字段：

```text
origin_date
model_vintage_id
training_end_date
b0_predicted_dsv5
b1_predicted_dsv5
q_b1_div_b0
input_sha256
prediction_generated_at
label_maturity_date
matured_actual_dsv5
b0_qlike
b1_qlike
b1_minus_b0_loss_improvement
```

禁止记录：

```text
target_weight
target_shares
buy_or_sell
order_intent
portfolio_return
```

前向只在52和104个新原点审查：

### 52原点初审

```text
B1相对B0的QLIKE改善 > 0
最近26原点改善不能明显为负
输入和模型版本无漂移
```

### 104原点复审

```text
前52原点改善 > 0
后52原点改善 > 0
整体Bootstrap下界 > 0
```

未通过则连B1观察器也归档。

---

# 二、现在不能继续做的研究家族

以下路径已经获得足够否定性证据：

| 家族                  | 当前裁决      |
| ------------------- | --------- |
| 当前20日波动率直接减仓        | 已失败       |
| 短中期波动率比直接减仓         | 已失败       |
| 成分广度直接映射仓位          | 已失败       |
| F/T成分脆弱性增加DSV5预测    | 已失败       |
| B1预测风险直接映射四档仓位      | 政策变化不足    |
| BAD10稀有压力事件模型       | 历史可评价时代不足 |
| 30/60日期权曲面风险预算      | 数据覆盖失败    |
| R6多状态综合仓位           | 已失败       |
| 技术、均线、Donchian、MACD | 已失败或冻结    |
| 高频日内T、VWAP均值回归      | 成本前后均不足   |

旧研究中，20日实现波动率择时完整历史Sharpe为负，短中期波动率比完整历史Sharpe也只有约0.31，说明“风险高就减仓”的简单翻译本身没有稳定经济价值。

成分股广度、领导差、共同运动、广度坍缩、左尾扩散和共同运动加速，也已经完整进入Stress Transmission与本次DSV5研究，不得再拆开重组成为新的状态过滤器。 

历史上曾出现过高Sharpe的微观结构和估值结果，但独立后段和2026时间复制没有延续，不能将其重新拉回组成“集成模型”。

---

# 三、下一项唯一值得运行的历史Alpha检验

当前建议：

```text
MODEL_ID
= 510300_NBS_1000_NEGATIVE_INFORMATION_DRIFT_V2

ROLE
= MACRO_EVENT_SATELLITE_ALPHA

TRADABLE_UNIVERSE
= 510300.SH + CNY_CASH_ONLY

PARENT_V1
= ARCHIVED_BLOCKED_BEFORE_RETURN_READ

PARENT_RETURN_VALUES_READ
= FALSE

LIVE_TRADING_AUTHORIZED
= FALSE

POSITION_IMPACT
= 0
```

这是目前少数仍然满足以下条件的方向：

* 机制与旧技术、波动、广度模型正交；
* 已有完整2017—2026分钟数据；
* 已有国家统计局官方事件账本；
* V1没有读取任何事件后收益；
* V1失败来自来源准入指标错位和时代切分问题；
* 修正不需要改变信号、窗口、目标或交易方向。

它不是对看过收益结果后的Alpha救援，而是**在结果读取前修正数据合同与样本切分**。

不过必须明确：

> NBS策略的定位是卫星模块。即使有效，也不太可能单独将全年510300组合Sharpe从较低水平提高到1.2；它需要证明能够稳定提供正的Sharpe增量。

---

# 四、NBS V2只允许修改两个地方

## 修改一：分钟数据准入合同与实际用途对齐

V1把以下日线一致性作为硬门：

```text
分钟high最大值 = 日线high
分钟low最小值 = 日线low
最后一分钟close = 日线close
```

但策略真正使用的只有：

```text
trade_time
vol
amount
窗口VWAP
```

而且你已经验证：

* 2,350个开放日全部有241根；
* 成交量和成交额日级聚合误差接近零；
* 88个事件的关键窗口100%完整；
* 时间戳语义为`BAR_END`；
* 2021—2026与既有分钟源逐字段匹配。

因此V2的硬准入门冻结为：

```text
开放日标准241根覆盖率 >= 99.9%

事件日四个窗口完整率 = 100%

重复ts_code + trade_time = 0

非法交易时刻 = 0

分钟成交量汇总与fund_daily相对误差 <= 0.1%

分钟成交额汇总与fund_daily相对误差 <= 0.1%

每根分钟VWAP位于：
[bar_low - 0.001, bar_high + 0.001]

2021-08-12至2026-08-12
与独立既有分钟数据的关键窗口VWAP：
匹配率 >= 99.9%

BAR_TIMESTAMP_SEMANTICS = BAR_END
```

以下保留为诊断，不再作为硬门：

```text
日线high与分钟high是否完全一致
日线low与分钟low是否完全一致
日线close与最后一分钟close是否一致
```

这不是降低质量，而是验证策略真正使用的测量对象。

---

## 修改二：评价时代等数量切分

V1已有：

```text
首次训练事件 = 36
严格评价事件 = 50
```

V1采用20/20/10，并要求每个时代至少15个，导致第三时代机械失败。

V2冻结为：

```text
ERA_1 = 评价事件1—17
ERA_2 = 评价事件18—34
ERA_3 = 评价事件35—50
```

即：

```text
17 / 17 / 16
```

这必须在读取任何事件后收益前完成。

---

# 五、NBS V2完整策略

## 1. 事件范围

只纳入：

```text
国家统计局
“国民经济运行情况”
事前日程明确为10:00发布
且当日为上交所开放交易日
```

继续排除：

* 13个无法证明事前日程版本的事件；
* 非10:00事件；
* 非交易日事件；
* 分钟窗口不完整事件；
* PMI、CPI、PPI、工业利润等其他事件家族。

预计保持：

```text
日程与窗口合格事件 = 88
具备60日基线的模型事件 = 86
```

不能为了增加样本纳入9:30事件。

---

## 2. 分钟时钟

由于数据是`BAR_END`，真实区间和分钟标签必须建立固定映射。

### 事件前窗口

真实区间：

```text
[09:55:00, 10:00:00)
```

对应BAR_END标签：

```text
09:56, 09:57, 09:58, 09:59, 10:00
```

### 事件反应窗口

真实区间：

```text
[10:00:00, 10:05:00)
```

对应：

```text
10:01—10:05
```

### 执行窗口

真实区间：

```text
[10:06:00, 10:11:00)
```

对应：

```text
10:07—10:11
```

### 恢复窗口

真实区间：

```text
[14:50:00, 14:55:00)
```

对应：

```text
14:51—14:55
```

窗口VWAP：

$$
VWAP=\frac{\sum amount}{\sum volume}
$$

单位换算必须先统一，不能混用股、手、元和千元。

---

## 3. 唯一信号

事件日价格反应：

$$
g_t=
\ln
\left(
\frac{VWAP_{10:00-10:05}}
{VWAP_{09:55-10:00}}
\right)
$$

用事件日前60个合格非事件交易日的同一时间窗口，计算：

$$
m_t=\operatorname{median}(g_d)
$$

$$
s_t=1.4826\times MAD(g_d)
$$

$$
z_t=\frac{g_t-m_t}{s_t}
$$

唯一特征：

$$
X_t=\max(-z_t,0)
$$

禁止加入：

* 成交量；
* 价格路径效率；
* 波动率；
* B1 DSV5预测；
* 成分F/T；
* 期权；
* 汇率；
* 社融；
* 技术指标。

---

## 4. 预测目标

$$
Y_t=
-\ln
\left(
\frac{VWAP_{14:50-14:55}}
{VWAP_{10:06-10:11}}
\right)
$$

含义：

* \(Y_t>0\)：事件负向信息继续扩散，上午卖出并于下午买回有价值；
* \(Y_t<0\)：盘中反弹，卖出产生机会成本。

标签在事件日14:55后成熟。

禁止更换为：

```text
11:30退出
13:30退出
15:00收盘
次日开盘
未来最低价
```

---

## 5. 模型

### B0

$$
\widehat Y^{B0}_t
=
\text{此前全部成熟训练事件的平均}Y
$$

### B1

$$
Y_t=\alpha+\beta X_t+\epsilon_t
$$

冻结：

```text
OLS
包含截距
HC3稳健协方差
首次训练事件36
之后每个事件前扩展窗口重估
不施加beta非负约束
不网格搜索
```

如果：

```text
beta <= 0
```

则直接失败，不允许反向改为“负向冲击后买入反弹”。

当日预测：

$$
\widehat\mu_t=\widehat\alpha_t+\widehat\beta_tX_t
$$

单侧90%下界：

$$
LCB_t=
\widehat\mu_t-1.645SE(\widehat\mu_t)
$$

---

# 六、预测门与交易门

## G2：事件机制

全部通过：

```text
B1相对B0严格前序MSE改善 > 0

beta全评价期点估计 > 0

事件Bootstrap：
beta单侧90%下界 > 0

三个时代至少两个：
B1误差低于B0

最近时代：
B1误差低于B0

Spearman(X,Y) > 0
```

## 两个固定安慰剂

### 安慰剂A：匹配非事件日10:00

每个NBS事件机械匹配前一个合格非事件日，使用完全相同窗口。

### 安慰剂B：事件日错位窗口

在同一事件日使用一个事前冻结、与10:00发布无关的等长盘中窗口。

要求：

```text
NBS真实事件X→Y关系
显著强于两个安慰剂
```

否则：

```text
REJECTED_MECHANISM_NOT_NBS_SPECIFIC
```

---

## G3：高安全边际信号

只有：

```text
X_t > 0
beta_hat_t > 0
LCB_t >= 42bp
```

才形成理论信号。

42bp约为14bp基础完整往返成本的3倍。

要求：

```text
独立信号 >= 12

至少两个时代各有 >= 3个信号

基础成本后平均避免损失 > 0

压力成本后平均避免损失 > 0

事件Bootstrap压力成本后
单侧90%下界 > 0

净命中率 >= 60%

任一事件贡献 <= 总净收益30%
```

信号不足：

```text
NO_VIEW_INSUFFICIENT_HIGH_MARGIN_SIGNAL_DENSITY
```

不能降低42bp。

---

# 七、一次性账户测试

只有G0—G3通过才允许运行。

## 交易规则

默认：

```text
事件前持有周转已满足T+1的510300旧库存
```

有效信号日：

```text
10:06—10:11
卖出全部可卖旧库存

14:50—14:55
使用可用现金买回510300
```

账户：

```text
资金 = 200,000元
佣金 = max(成交额×0.0002, 5元)/边
整手 = 100份
基础滑点 = 5bp/边
压力滑点 = 10bp/边
现金收益 = 0
```

买入后的新份额当日不得再卖出。

## 基准

```text
P0 = 510300含分红买入持有

P1 = NBS事件策略

P2 = 相同次数、相同时长的匹配非事件日现金窗口

P3 = 相同事件日但时间错位的现金窗口
```

## 晋级门

NBS不是独立核心，因此不要求它单独达到全年Sharpe 1.2，而要求：

```text
P1相对P0累计净超额 > 0

P1净Sharpe - P0净Sharpe >= 0.10

P1相对P2净超额 > 0

P1相对P3净超额 > 0

基础与压力成本下均为正超额

三个时代至少两个净超额 > 0

最近时代净超额 > 0

全年CAGR下降不超过0.5个百分点

最大回撤不能恶化

单一事件贡献 <= 30%
```

通过后身份只能是：

```text
VALIDATED_HISTORICAL_SATELLITE_CANDIDATE
```

不能直接实盘。

---

# 八、最终高Sharpe系统的现实路径

目前不存在经过验证的核心择时模型。因此最终系统不能继续依赖一个越来越复杂的“主模型”。

更现实的结构是：

```text
510300长期风险溢价
        +
少数独立、低频、机制明确的风险回避模块
        +
ETF执行成本优化
```

候选模块顺序：

| 模块          | 当前状态          | 作用            |
| ----------- | ------------- | ------------- |
| 510300基础持有  | 基准            | 获取长期权益风险溢价    |
| NBS 10时漂移V2 | 下一项一次性检验      | 回避部分宏观发布日剩余风险 |
| A50盘后过时价格   | 严格前向、数据条件尚未满足 | 捕获盘后跨市场信息差    |
| PCF/IOPV    | 前向采集中         | 降低执行摩擦，不预测方向  |
| B1 DSV5     | 只读观察器         | 风险监控和未来模型基准   |

任何模块必须独立通过，再进入组合。不能先把几个弱信号拼起来，然后依靠组合Sharpe掩盖各自无效。

---

# 九、接下来的执行顺序

## 立即执行

```text
1. 归档DSV5政策V1。
2. 保持dsv5-g1任务PAUSED。
3. 新建只读B1风险预测观察器。
4. 冻结STK_MINS_SOURCE_ADMISSION_V2。
5. 冻结NBS_NEGATIVE_INFORMATION_DRIFT_V2。
6. 保留V1所有文件和哈希，不覆盖。
```

## NBS V2数据阶段

```text
1. 使用既有566,350根分钟数据。
2. 只验证time/vol/amount/VWAP合同。
3. 重建86个合格事件的无收益账本。
4. 冻结36/17/17/16切分。
5. 完成Git提交和one-shot claim。
```

## 预测阶段

```text
1. 运行B0。
2. 运行B1严格前序预测。
3. 运行两个安慰剂。
4. 只读取G2预测结果。
```

## 经济阶段

```text
G2通过
→ 读取42bp信号密度与事件净边际

G3通过
→ 一次性运行20万元账户

失败
→ 永久冻结NBS家族
```

---

# 十、停止线

NBS V2失败后，不再继续做新的510300历史日线、分钟线、广度、波动率或宏观参数组合。

届时主动研究转换为：

```text
STRICT_FORWARD_ONLY
```

只保留：

* PCF/IOPV；
* A50盘后信息时差；
* 宏观首次发布vintage；
* B1风险预测观察；
* 实际券商执行成本。

---

# 最终决定

当前最合理的动作不是调整DSV5仓位边界。该政策在历史上几乎等于买入持有，改变边界会成为典型的事后救援。

正确顺序是：

```text
关闭DSV5政策
保留B1为只读风险基准
暂停原19:30政策任务
执行唯一尚未读取收益的NBS V2宏观事件实验
继续PCF/IOPV等严格前向基础设施
```

**NBS V2应当是当前最后一项510300历史Alpha主实验。** 它通过，作为卫星模块进入严格前向；它失败，历史Alpha挖掘停止，后续只做未污染的前向制度型和执行型研究。
