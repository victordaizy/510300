# 510300 CSP V1 静态底仓与择时归因：冻结协议

任务：`510300_CSP_V1_STATIC_VS_TIMING_ATTRIBUTION_V1`，版本1.0.0。研究阶段为 DIAGNOSTIC_ONLY；原研究及主历史已经看过，本次冻结只约束新增诊断的实施，不制造未观察样本。原 V1 拒绝结论和净夏普1.2目标继续有效。

## 依据与收到的材料

用户提交的外部GPT审阅正文已原样保存至 `docs/510300_CSP_V1_EXTERNAL_REVIEW_USER_TEXT_20260906.txt`。正文第九节给出本次任务的账户、预算与停止规则。本地没有收到正文链接中的外部审阅ZIP、独立任务书及复算证据文件；不能声称已读取它们。本协议将正文未详细规定的余单、理想路径、统计与核算定义补全，全部在新增四条历史账户运行之前固定。

外部审阅报告其环境34项测试通过、另1项缺少Parquet依赖；原本地冻结回执记载35项通过。这是两个环境的记录，不改写任何一份原始回执。外部声称的108条验证复放和特征复算等不计入本轮实际完成量。

## 固定账户与时间

只新增 C0_BASE、C0_STRESS、C1_BASE、C1_STRESS 四条可交易规则模拟账户。每条从20万元现金、零份额、零应收开始，2019-12-31收盘形成初始请求，2020-01-02开盘首次尝试执行，至2026-08-14收盘估值，1,604个交易日完整保留，现金利率0。原FULL、BUY_HOLD、SIMPLE、NO_REPAIR只读取保存账本及统计，不重跑账户或拟合。资产仅510300.SH与CASH_CNY。

S0直接读取原 `development/selected_models.json` 中FULL的全精度截距 b0，计算 S0=100/(1+exp(-b0))，w0=clip((S0-20)/60,0,1)。不得使用正文显示的小数替代原始精度，不估计新截距。

- C0：在锚点按 floor(w0×200000/收盘价/100)×100 冻结份额。若开盘未成交，原请求延续；一旦出现首次正成交即视为初始建仓完成（现金不足造成的部分成交也算完成，放弃剩余份额），此后不再交易。分红留现金，不再投资。此规则与原买入持有部分成交终止惯例一致。
- C1：每日评分恒为S0，使用原 `choose_request` 的10个百分点不交易带、25个百分点单日加仓上限、实际账户暴露反馈、100份取整、有效新收盘替代旧请求以及NO_VIEW保留旧余单。沿用保存特征中的有效性掩码，不计算任何新指标。末日收盘决策仅记账，不执行窗外交易。
- 两类都直接调用原共享Account与execute_order；原佣金、最低5元、逆向滑点和0.001元价位、涨跌停保守规则、T+1、现金不足缩量均不改变。分红登记日收盘锁定份额，除息日开盘计入应收，付款日收盘兑付，非交易付款日于下一开盘兑付。锚点账户为现金，无历史分红权利。

新增调度器的开盘判断只读取当日开盘价、前收盘、当日已知除息额以及已核对的交易日期，不读取当日最终成交量、成交额、最高价、最低价或收盘价来判断开盘成交。日线数据仍不能证明历史开盘每一笔真实可成交，涨跌停与费用也仍是原研究假设。对原代码使用全日成交量的问题，仅检查本样本旧/新判断是否等价，不修改或重新运行原V1。

## 数据和账本门

仅用原配置列出的5个输入文件、原保存特征有效性掩码、系数、账本与回执。冻结时核对原输入/实现及开发产物哈希，记录原结果树每个文件的身份。无下载、填值、删日、延长历史或新增指标。

新增账户运行前先核对1,604日历、价格/单位、分红覆盖与日期，以及原8份保存账本的人民币财富恒等式、现金流、份额、手续费、逆向滑点、T+1、分红登记资格/应收/付款、逐日收益和日期。以保存成交重建会计，不调用原模拟器重放。任何不一致停止，保存失败回执，不修改原输入。

新增每条账户只运行一次；即时核对财富守恒，并在统计前使用同一独立会计核算器逐行核对。原样保留失败产物，不能换参数或修改原数据重跑。

## 四条理想统计路径（本地补充定义）

正文允许“同平均暴露、同波动”最多4条理想路径但未指定参考及公式。本次只选原保存BUY_HOLD作为参考，在BASE/STRESS分别固定两种比例：

1. kE=mean(FULL每日收盘暴露)/mean(BUY_HOLD每日收盘暴露)。
2. kV=std(FULL净日收益,ddof=1)/std(BUY_HOLD净日收益,ddof=1)。

理想日收益 rI,t=k×rBH,t，理想财富 WI,t=200000×∏(1+rI,t)。参考暴露 xI,t=k×xBH,t；kE使其全期平均值与FULL完全一致，kV使净日波动与FULL完全一致。费用和分红只通过原BUY_HOLD净收益被同比例缩放，不另增真实订单。保留实际k，禁止挑选、截断或根据结果换参照。

这些是每日线性混合参考净收益与零收益现金的统计路径，涉及事后全样本匹配、隐含连续调整及比例费用，忽略额外调整摩擦，也不满足真实最低佣金和整手合同。xI是统计参考暴露，非真实股份台账。两种标准化各匹配一个量，并不同时匹配暴露和波动，也不控制全期时变风险。不得称为可交易策略、精确预测归因或未来参数。

## 差额与一次联合区块统计

实账户主比较为 C1−C0（固定评分下机械再平衡差异）、FULL−C1（允许状态变化后的整体政策差异）及FULL−C0。机械路径本身也有时序和暴露变化；FULL−C1不能进一步当成已去除全部风险差异的纯预测alpha。

人民币分解在各日采用：FULL权益−初始本金=(C0权益−初始本金)+(C1权益−C0权益)+(FULL权益−C1权益)。三个部分严格相加，末日点估计以元和占初始本金比例披露；不同账户复合年化率之差只是比较量，不作为人民币归因百分比。机械部分为零时不能称检验了普遍再平衡效果。

统计固定使用原种子20260906、20日循环移动区块、2,000次，只运行一次联合重采样；所有被比较日收益列共用同一索引（保存索引和抽样统计）。把新四账户、原FULL/BUY_HOLD四份保存路径和四条理想路径一起纳入。匹配比例k在bootstrap内保持固定，所以区间条件于本次事后匹配，并不纳入比例估计及整个研究选择的不确定性。不存在策略再训练。

每个成本档报告前述3个实账户差额及FULL−IDEAL_EXPOSURE、FULL−IDEAL_VOLATILITY，指标包括终值人民币差额、复合净年化差、净夏普差、gamma5效用差与算术年化差，各自同区块95%百分位区间。保存各单独路径指标和区间。区间不做历次研究/多比较校正，不是未来成功概率；理想路径区间不代表可交易收益。

仅复算原FULL−SIMPLE的已保存区间并修正解释，不额外运行原统计。净年化差跨零与净夏普/效用差不跨零应分别如实记载。

## 工程预算与终态

新拟合0、优化器0、参数网格0、新账户4、理想路径4、联合区块一次、新数据下载0、工程最多1工作日。合成测试不接触真实行情，不计为新增历史账户；禁止以测试之名新增真实回测。入口一次性claim与冻结清单约束执行，保存耗时及预算回执。

无论差额正负，终态 `COMPLETED_DIAGNOSTIC_ONLY_NO_PROMOTION`。不晋升C0/C1，不自动继续日线权重/带宽/窗口/种子搜索；没有新的达标候选。原V1冻结拒绝、旧分支裁决均保持；Paper/Shadow、券商接入、订单及实盘未授权，真实持仓未知、仓位影响0。

交付一个完整GPT审阅ZIP：本协议、用户审阅正文、源代码/测试/配置、4条新增账户、4条理想路径、会计与输入门、解析边界、联合区块样本、报告、一次运行回执，以及原研究完整上下文。只做必要的结构与可复算性检查。
