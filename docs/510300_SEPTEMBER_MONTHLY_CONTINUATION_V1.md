# 第214轮：补一次原定月首训练，直接从八月末状态接到9月11日

本项必要实现及测试已完成，现于首次新增训练及增量账户之前固定。213已完成22条必要账户至8月31日的连续收盘结算，原8月13日及之前正常区间一致。保存38742条账户记录和38764条决定；实际持有64200份的学习退出来源经过8月13日JSON状态中断与恢复，数值、决定和最终控制器状态一致。原10项必要测试已通过。

213第一次整批计算完成后，真实恢复比较因无成交价格空值的列类型不同而中止。已由 scripts/complete_round213_saved_20260913.py 直接读取保存结果完成核对与汇总；没有重跑22条账户。research/post_selection_continuous_replay_v1.py 的 prepare/run/verify 均已结束，不重跑；实际状态以 result.json、saved_verification_receipt.json 和 actual_resume_verification.json 为准。

## 固定任务和最快接续方式

继续固定209的85%与15%两来源方案、十个百分点调仓门槛，不优化权重或其他参数。只生成2026年9月1日原月首日程应有的两条模型记录，然后把22条固定来源账户从213的8月31日状态接到9月11日。使用212已保存的3476日候选输入和官方分红覆盖，不重新请求网络。

计划零新候选、两次原定月度拟合、22条必要账户的九月增量续接，另有一条原训练基准参考的必要连续恢复。最后一项是延长31原来的基准参考，不是新增一种参考策略。旧141条岭回归模型和旧141条持仓内模型全部直接复用，新条目附在独立新文件中。

新统一入口 research/september_monthly_continuation_v1.py，隔离输出 reports/research/510300_september_monthly_continuation_v1。复用213连续账户引擎、各节点纯公式和27节点依赖图；新增一个不带再入场锁定的原学习基准参考连续适配。只针对实际需要的日期、原训练记录和状态接续做必要验证，不再拆出一轮资料或依赖盘点。

## 原训练基准与成熟样本

31的原基准程序是 research/learned_cycle_exit_account_v1.py 中 simulate_learned_exit，训练抽样定义在 research/learned_cycle_exit_v1.py 的 build_samples、state_values、continuation_label、training_rows。该基准与91的 REARM_RIDGE 连续参考不同，不能互相代替。只延长六十日日内强于隔夜信号对应的那一条基础费用参考，从原2013年6月3日起点恢复或最小必要重放；其他两个信号和树模型不计算。

先核对原基准8月13日及之前的正常账本及已成熟样本。原8月14日人工终点退出继续排除；正在持有的周期要按原自然规则结束后才能成熟。记录持仓状态时，学习退出回调只记录原八项状态，不额外请求退出。原基准账本在 reports/research/510300_learned_cycle_exit_v1/reference，样本在同目录 all_reference_samples.parquet。

八项因素依次为：持有交易日数加一的自然对数、含分红持仓收益、持仓价值距离周期高点的回撤、入场模式、五日累计涨跌、二十日累计涨跌、一百二十日均线偏离、二十日年化波动。沿用原标准化、五倍截断、岭惩罚一及每个完整周期总权重一，不改因子定义。

训练只使用9月1日15:05之前已自然结束周期的状态；原定义中当前收盘后的下一开盘必须早于自然退出开盘，且原八项状态完整，才能计算“继续持有到自然退出相对下一开盘退出”的原标签。使用原 training_rows 函数在可用样本中选最近20个成熟周期，至少十个周期和一百行才拟合。禁止把9月2日以后退出的周期混入9月1日训练，禁止把研究终点清仓当成熟退出。

## 两条原定模型记录

第一条调用31原 fit_one 的岭回归分支，使用上述六十日日内来源的成熟样本；第二条调用 research/within_cycle_exit_inputs_v1.py 的 fit_within_cycle_exit，在同一组成熟样本、相同权重和八因素上生成持仓内模型。后者沿用每个成熟周期单独截距和等权平均的预测截距，不能改回其他标签或估计器。

两条记录保存原格式的拟合索引、拟合日、15:05时点、成熟周期编号、训练行数、最晚退出日、系数与失败状态。把新记录附在旧模型序列之后；旧记录逐条精确相同。支持不足保留原无模型状态；拟合异常记录实际失败，不换求解器、不重新调参数。139的模型支持选择必须与实际新记录状态一致；若原适配不接受新增失败状态，要明确产生资料不足状态，不冒充成功。

## 九月只算增量账户

读取213每个 accounts/费用/来源/checkpoint.json，保留现金、份额、T+1买入批次、分红登记权利、应收分红、上次计价、待执行申请和周期／控制器状态。9月1日开盘先执行8月31日已经形成的申请；9月1日收盘后才可使用当日新模型。128来源若已有持仓，其首次买入锁定的模型保持到该周期结束；不能因为九月新模型出现就切换原持仓版本。

纯因素、日数计数和月度预算可用完整已知历史重算，但仅将9月1日至11日的新增账户记录接在213账本后，不重放原22条完整账户。213已有部分月份预算和全部来源目标文件；以原纯公式及必要历史收益窗口补齐新目标，保存各层目标与使用的模型时点。

本轮应补齐“整条来源图的增量续接验证”：至少对主压力方案及其必要状态按统一入口核对增量拼接的旧前缀不变，父来源实际收益和计划份额使用当时状态，账户财富恒等式成立，最后真实收盘决定的下一交易日由官方日历给出。213只验证了一条真实周期来源和三类引擎的断点恢复，不能把它写成整图增量已经验证。

## 交付与剩余证据

报告新增两条模型记录的成熟周期、训练行数及中文系数规则；报告主方案至9月11日的两费用完整连续账户指标，分别列8月14日至8月31日、9月1日至9月11日的实际收益与交易。短延续段不强行年化，也不通过切换窗口维持1.2。若指标跌破门槛照实保留，不能悄悄另选209的其他组合。

213固定方案至8月31日：基础夏普1.2883747、年化10.9736683%；压力夏普1.2239093、年化10.3737055%，两账户均按信号在8月21日完成退出，8月31日空仓。这些仍是补充历史结果，209原四情景历史点值通过保留。

所有新增行情发生在209正式冻结2026年9月13日之前，严格前瞻交易日仍为零。补齐九月后才能按完整当前状态准备冻结之后的新研究决定记录；不启动券商、订单、实盘、Paper、Shadow或新任务。使用简短中文MD说明完整进出场与因素，不制作GPT包、ZIP或重复Word。
