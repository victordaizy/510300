# B1 DSV5 只读风险预测观察器

观察器身份为 `510300_B1_DSV5_FORECAST_OBSERVATORY_V1`，职责为 `RETAIN_AS_FORECAST_BENCHMARK_ONLY`。DSV5 仓位映射保持 `DISABLED`，不存在当前已验证高 Sharpe 策略，仓位影响为 0。

工作日北京时间 19:30 触发。日期属于以 2018-11-21 为锚点的固定五交易日主网格时，才尝试生成新预测；其他日期为 `NOT_ORIGIN_NOOP`。第一新原点为 2026-09-11，假设官方日历不变，首个标签于 2026-09-18 收盘后成熟。历史漏跑原点不会补造预测。

预测仍采用父 B1 的三个价格特征 LOG_RV20、NEG5、DD20，以及指数链接、非负斜率、QLIKE Ridge、L2=1。B0 为成熟训练标签均值。训练种子仅为父研究已冻结的 offset 0 风险标签；不重新运行父历史训练或评价入口，不加入前向登记之前遗漏月份的新标签。第一次新原点及其后每 13 个固定原点重估一次；中间原点保持该版本。只增加已经在此观察器真实记录且标签成熟的新原点。不会加入 F/T、NBS 或仓位政策。

配置的 13 个预测字段构成固定输出表。初次预测时实际 DSV5 和损失字段为空；标签成熟后写入独立到期记录和版本回执。`b1_minus_b0_loss_improvement` 明确定义为 B0 QLIKE 减 B1 QLIKE，正值表示 B1 改善。原始预测文件不会被到期值覆盖。导出 CSV 可以随合法到期更新。

只在前 52、104 个新网格原点的标签成熟时审查，缺失预测不挪动审查时点。52 原点时总改善须为正，最近 26 原点改善的单侧 90% Bootstrap 上界不得低于零，以此定义“不能明显为负”；104 原点时前后两段 52 原点改善均须为正，整体单侧 90% 下界须为正。Bootstrap 沿用 13 原点循环区块、5,000 次、种子 20260905。任何审查失败或有效样本不完整时归档观察器，不能降门或补填历史样本。

输入来自独立目录的当日 fund_daily 原始抓取和官方分红覆盖回执。不会覆盖旧日线或源数据。每天的历史价格和已证明完整的分红历史若发生变化，输出 `NO_VIEW_*_DRIFT`。源凭据只从进程环境或 `.env` 临时读取。来源不具备当前日期、官方分红覆盖不足、未到原点、标签未成熟、模型版本缺失时，均不产生预测。

截至登记时，官方分红覆盖证明只到 2026-08-14，官方交易日历覆盖到 2026-12-31。到期前须由相应来源维护流程取得新的官方分红覆盖证明，以及跨年度时的新官方交易日历版本；程序不会自行延长证明日期。当前登记和自动任务启用只代表观察器已经就绪，不代表已有严格前向预测。

运行入口为项目 `.venv/Scripts/python.exe scripts/run_510300_b1_dsv5_forecast_observatory_v1.py tick`。`status` 入口禁止在线抓取，仅检查当前日期状态；生产入口不接受历史日期或回拨时钟参数。自动任务遇到新预测、新成熟结果、里程碑结论、程序失败或首次需要处理的输入缺口时通知；状态不变及非原点静默。
