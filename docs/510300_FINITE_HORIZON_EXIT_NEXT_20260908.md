# 第117轮后的直接下一步：有限持仓期限逐期退出

第116、117轮已经结束。116成交量仅令主基础夏普从0.884微升至0.896，但压力从0.831降至0.700，较早也下降，不保留该新增信息。117学习状态分离已付买入费用，消除了相同市场路径的机械费用敏感性，但四个真实账户与114净值逐日完全相同，没有收益增量。114原固定方法继续只是局部候选，目标没有达到；当前没有运行中的拟合或账户。

第116轮2021年2月18日的变号全部来自含成本浮盈亏，具体分解已保存在cost_state_sensitivity.json；117的381个跨费用可比预测已验证一致。不能把这个例子当成所有失败的因果解释，也不能再给116改成交量窗口或给117混合不同费用价格基准。相关结果已核，不重复运行。

## 已完成的逐期成熟支持

[支持回执](<C:/Users/戴周阳/Documents/New project 8/reports/research/510300_finite_horizon_age_support_20260908/result.json>)及同目录两张中文表已经落盘。原1461个训练状态的持仓年龄为1至59，按原114个成熟月份、原最近二十完整周期拆分，共6726个“月份×持仓年龄”单元；5685个至少十个独立成熟周期，1041个不足。跨月重复的90217条成员状态中，81650条处于这些有十周期支持的年龄单元；这些重复成员不是90217笔独立交易。

最早2017年3月1日的十周期438行，只有持仓第一天达到十周期支持。最后2026年8月3日的二十周期790行，第1至42天达到十周期支持。每个年龄最多二十行，所有单元都不可能有一百行。若采用新结构，必须明示“每个年龄至少十个独立周期，原至少一百行只针对整个月度窗口”，不能宣称每个小模型仍有一百行。支持不足的年龄不降门槛、不借用未来年龄或其他月份结果。

这次仅统计成员和日期，没有新拟合、新账户或新参考。无需重复支持检查。

## 拟定的有限比较，尚未登记

方法依据可参考[Longstaff与Schwartz原论文的逐期继续价值估计](https://people.math.ethz.ch/~hjfurrer/teaching/LongstaffSchwartzAmericanOptionsLeastSquareMonteCarlo.pdf)。已阅读论文的逐期回归现金流示例；加州大学页面当前要求浏览器验证，正文使用上述论文副本。论文是期权定价，不证明本510300研究盈利。本次如果采用的是已经成熟的真实历史参考周期，不生成假想随机价格，不增加期权或其他资产权限。

拟比较两种设置，使用相同的年龄划分、信息、支持条件和实际退出确认：主要设置按持仓年龄从后向前，回归训练周期未来遵循已估计退出选择的实现现金流；对照在相同每个年龄回归到原自然退出的固定目标。这样可以区分逐期模型和更新后续退出选择的贡献，不靠更换确认次数解释收益。

这与原52把所有年龄混在同一八因子回归、固定六十次自举迭代不同；与原96在空仓机会组上比较买入和等待、反复迭代至稳定也不同。两份原协议已经阅读，有界Longstaff/Schwartz/LSMC/逐持仓日倒推检索未发现既有实现。不得重新扫描旧52的迭代次数、确认次数或惩罚。

可复用117已经验证的每份市场路径两因子，结合持仓年龄、模式及四项原市场因子；其固定定义本身不再调。每个年龄同一周期最多一行，拟用每周期相同权重。标准化、正则化、未来现金流单位、原完整周期成熟、支持不足时行为，必须在新规则中明确。年龄在同一小模型里是常量，不应假装它在该模型中存在横截面变化；每周期单独截距在一个周期仅一行时也不能再作为114那样的周期内估计。

训练路径的后续退出日必须由已训练的决策选出，不能挑未来实际利润最大的日子。未来价格和分红只用于该月已完整成熟的训练目标。原自然退出是不能跨过的边界；没有该年龄模型时，保持预设的原自然退出行为，不把缺失预测写成零。单次或两次确认必须与倒推选择一致，应优先明确一个理论一致的退出规则及同样确认的对照，再冻结。

需要处理请求退出后的可成交时点：训练标签不能在明显受方向涨跌停阻挡的原开盘假装成交。可对每条已成熟参考路径预计算“该收盘请求后，第一可卖开盘及其净收入”，使用原基础费用、真实分红登记以及次日可卖规则，并保留退出锁定。它是成熟训练结果，不是当前预测输入，不新增参考买入策略。尽量将共同设计矩阵、价格和分红现金流表复用，两个目标同一年龄共用求解，控制计算量。

第118轮尚未创建模型、测试或冻结配置，也没有任何该方法的收益。先实现一个有限方案与必要测试，再冻结并跑主／较早、基础／压力的全部真实账户和同时登记的对照。实际账户仍独立现金、份额、全部费用、原价格风险保护和再入场规则，不拼接路径。

继续只研究510300与现金，原两历史区间、费用和经济口径保持；其他ETF问题已问未答不重问。EPS、公募及慢源暂停，无GPT审阅包或额外安全审计。目标active，上一次及本次goal回合均为实际进展，没有阻塞条件。短交付与索引使用fast_round_delivery_v1.py，旧详细提示只按具体问题追溯。
