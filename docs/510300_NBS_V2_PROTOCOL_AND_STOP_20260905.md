# NBS V2 冻结协议及停止线

用户原始要求保存在 `510300_NBS_V2_USER_REQUEST_20260905.md`，可执行阈值及完整门槛保存在 `config/510300_nbs_1000_negative_information_drift_v2.yaml`。该版本属于宏观事件卫星 Alpha，执行资产范围为 510300.SH 与 CASH_CNY，实盘授权为 false，仓位影响为 0。

V1 原件及其无收益停止回执保持不变。V2 仅调整分钟来源的测量合同和评价时代等数量切分；用户随后明确回复“允许，仅补齐安慰剂 B”，授权修补 V1 未定义事件前价格基准的问题。V1 的 09:30 反应窗口缺少之前五根连续交易分钟，因此 B 的四个真实区间统一提前主实验十五分钟：09:40—09:45、09:45—09:50、09:51—09:56、14:35—14:40。所有窗口均为五分钟，现金持续时间不变。该补齐不修改主实验窗口。

分钟适配器按用户指定 BAR_END 标签工作：左端加一分钟作为首个标签、右端作为最后标签。普通行情文档对端点的开闭描述与用户真实区间记法有差异，实现在分钟层只使用冻结标签，不从分钟数据推断秒级发布边界。

NBS V2 的唯一特征、目标、60 日前序基线、36 事件初训、OLS 截距和 HC3 均固定。预测下界系数保留用户明确指定的 1.645；事件 Bootstrap 单侧下界按第 10 百分位。全评价期 beta 只用 50 个评价事件估计，两个安慰剂的优势检验采用对应事件成对重采样的 beta 差下界，两项都须大于零。不能通过剔除退化重采样、换种子、改阈值或反向信号救援。

时代固定为训练 36，随后 17／17／16。88 个日程及窗口合格事件、86 个有足够基线的模型事件沿用原身份与排除规则，13 个事前日程版本不可证明事件继续排除。账户规则及卫星晋级门按用户本次完整要求覆盖 V1 的旧账户基准，不沿用 V1 的独立 Sharpe 1.2 目标。

执行顺序为 G0 来源 → G1 无收益事件账本 → 冻结提交及一次性尝试登记 → G2 预测机制 → G3 42bp 信号 → G4 账户。来源失败时禁止标签、预测、两个安慰剂的收益关系、信号密度、事件净边际及账户结果读取。数据资格失败不能写成 Alpha 统计失败。

本次来源测量在提交 `326e345c9673246d5c6dbbab7912de1cb5dbcade` 后执行。263 根分钟的 amount/vol 越出 low/high ±0.001 元，直接违反用户的逐根硬门；232 根零量且零额分钟无可定义 VWAP。零成交分钟按缺失记录，即使不将其作为额外停止理由，263 根明确越界仍足以阻断 G0。没有修改金额精度、扩大容差、删除分钟或改为只核对事件窗口。

因此 V2 以 `ARCHIVED_BLOCKED_G0_BEFORE_RETURN_READ` 终止。G1 的 36／17／17／16 切分通过；G2、G3、G4 均不运行。一次性登记记录的是已终止的研究尝试，标签读取权从未消耗，且禁止借此重新开启本家族。

后续 510300 主动研究切换为 `STRICT_FORWARD_ONLY`。允许的研究范围为 PCF/IOPV、A50 盘后信息时差、宏观首次发布 vintage、B1 风险预测观察及实际券商执行成本。各项既有任务继续遵循其独立版本与授权；本次不把来源状态描述成已采到新的前向数据，也不恢复旧失败程序。
