# 第128轮待实现：单笔持仓固定首次判断的退出模型

127新增当前六十日强弱的退出模型已完成114次拟合、四个实际账户和必要核对。主基础／压力净夏普0.769／0.719，较早0.764／0.740；主回撤17.02%／17.24%，没有稳定增量，关闭此新增因子。主2021年2月3日进入的持仓由原2月19日退出延至3月1日，基础该笔由盈利11215.21元变亏5459.66元；2026年另一笔晚一天有利，较早两笔分别不利及有利。不能据此给某年份另设规则。

## 下一项的具体区别

下一项回到原114八因子周期内模型，只检验一次实际持仓是否应固定使用第一次持仓收盘所选的模型版本。当前实际账户在持仓时每天取最近完成月份的模型；新方式在本笔实际买入后的第一个收盘选取当时已经完成的最近模型，直到实际全部卖出都使用这一版本。因子和实际盈亏仍每天更新，固定的是模型参数及其标准化。每笔新实际持仓重新选取当时最新版本。

第一次选择发生在实际买入日收盘，不能和买入申请前一收盘混淆，也不能提前使用开盘以后尚未形成的模型。没有实际买入不锁定。第一次选择无成熟模型，则本笔全程不补学习预测，即使后来成熟也不替换；原价格及时间保护照常。实际首次调用必须与买入日收盘一致，不能用后来第一次有效输入或任意当前日期重新设起点。

这不是将127的第九因子改窗口或改阈值。127关闭；114原八项与已保存模型、原训练支持、进入、重新进入、价格保护保持。它也不同于40：40锁定所选基础策略身份，策略内部月度模型仍按各自规则更新，本次固定单个原114策略持仓内部的预测函数版本。已查看40全文，并对相关退出与进入模块有界检索，命中的entry_model等是空仓进入模型记录，没有发现同一持仓版本锁定实现。

## 已有记录支持与边界

已比较原114保存的模型与实际持仓行，只比较实际预测函数的因子、训练均值、标准差、系数、平均截距及截断范围，不把仅模型日期变化当作参数变化。主两档各20周期中3周期存在与本笔初始模型不同的参数，共16个持仓状态；较早各9周期中2周期、47个状态。四账户合计126个原路径状态的参数身份不同。首次选择均来自当时可用的记录，没有计算新锁定版本预测、动作或收益。

预检已保存reports/research/510300_entry_vintage_exit_preflight_20260909/result.json，来源哈希、具体起点及参数差异均在其中。这个数量只描述原114路径，不能预言新账户一定改变哪些交易，更不能把127的主要2021年损失都解释为月度模型变化。后续真实交易才验证此独立改动。

## 直接实现与固定边界

复用reports/research/510300_within_cycle_exit_v1/saved_models.json的141条原记录，不重训114个成熟模型，不生成参考账户。原八因子当前实际持仓状态由原state_values生成，预测使用本笔固定的原within_cycle_prediction。新控制器在首次新cycle_id时固定record，并保存选择收盘、模型收盘及参数身份；每天更新当前输入和连续负预测计数。当前输入缺失时无预测、负计数清零，参数版本继续固定；原已锁定退出不撤销。卖完后下一次实际买入重新选择，不跨笔复用。

必要测试覆盖跨月出现相反新模型但本笔版本不变、下一笔获得新版本、初始无模型全笔保持无模型、当前输入缺失只影响预测和负计数、不能读取买入日收盘之后的模型，以及下一开盘退出和原保护退出。原114源码不修改；可以新增独立控制器并调用既有预测函数。主每段两费用共四新账户，复用114、32、91和买入持有16个保存对照，一套设置、零新拟合。

冻结前补完整中文八因子与进出场协议：原六十日强弱两收盘大于一入场、下一开盘真实可负担整手买一次；固定本笔模型连续两负、原强弱两收盘低于零、6%亏损、8%最高价值回撤、60日持有任一退出并保留受阻请求；卖完旧条件须消失再出现且等待两日。不要增加新止盈、改确认天数或重设冷却。

只研究510300与人民币现金、各期20万元、主1604日和早1219日、242年化、零现金及无风险收益、原两档佣金滑点、百份整手、一厘价位、次日可卖、方向限制、真实分红权益应收到账与最后开盘优先清仓。新旧账户分别计算真实经济路径，缺失不能当现金。

测试后只冻结一次，直接四账户，核对已保存参数身份、实际预测及完整周期。失败后关闭此首次持仓收盘版本锁定，不改为入场前一天、部分更新、定期替换、增加窗口或混合模型挽救。净夏普至少1.2、稳定超额与独立验证目标仍未达；已观察历史不是独立样本。继续暂停EPS及慢来源，无需GPT数值包、ZIP、安全审计或新交易权限。
