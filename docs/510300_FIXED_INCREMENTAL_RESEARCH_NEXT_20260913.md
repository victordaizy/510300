# 第215轮：固定当前研究状态，准备单次新增交易日续算

第214轮已经补齐两条原定九月模型，并把22条必要账户从八月末增量续到9月11日。下一项直接使用这一终态，不再扫描旧策略、不重新优化、不重跑已经验证的22条历史。

## 先完成能在开市前完成的工作

将214的22个研究账户状态、两条完整模型序列、依赖图、价格和分红输入身份以及各来源9月11日收盘已有的下一开盘申请，绑定为同一份研究延续起点。记录实际保存时间；已知最后价格日为9月11日，下一官方交易日为9月14日。该记录在9月14日结果产生之前保存，不能把模型的历史模拟时间当成本次实际保存时间。

它只是固定研究实验的延续起点和旧规则已经生成的申请，不连接券商、不向用户实际账户发出指令，不启动Paper或Shadow服务，也不改变实际持仓未知的状态。主方案两个费用账户目前研究持仓均为零，不代表用户账户为空仓；三个内部参考仍有持仓，必须保存全部22个状态，不能整体重置成现金。

必要入口复用213的连续引擎、214的有效决定起点比较和两条原月首模型。把新增起止日期、上一状态目录、新结果目录作为显式输入，禁止把9月固定九天或八月末起点写死。仅对新增日期恢复22个账户；原历史因子和风险预算需要的有限窗口可以读取，旧账户不重放。模型不在每日重复拟合，仅按原月首安排更新；已有持仓保持入场时锁定的模型。

## 免费新数据与单次处理

目前已接纳的最后行情为2026年9月11日；9月13日为周日，缺少新交易日收盘结果属于真实等待条件。下一次检查安排在下一官方交易日完整收盘以后。使用原新浪、腾讯和官方分红来源，保持正常TLS、原价格及成交量精度口径。不在没有新日线时反复请求或重新生成旧研究轮次。

仅接纳发生在冻结之后、取得完整日线和分红覆盖的新日期。每次保留实际取得时间和原始响应，旧接纳数据不覆盖。日期缺失、日线未结束、价格核对失败或分红覆盖不明时，该次新数据不进入账户；沿用已保存状态并记录明确缺项，不能用填充值伪造新观察。

9月14日开盘的历史模拟申请只能使用9月11日已经生成并在开盘前保存的研究记录。9月14日收盘产生的下一申请，必须记录真实计算及保存时间；若晚于下一开盘，不算及时的前瞻决定，不能回填时间。输入的新日期与原状态必须首尾相接。申请、成交模拟和收益评价分别记账，研究模拟成交不称为真实成交。

## 最小必要验证与结果

只验证一次通用新增日入口：保存状态恢复后首个开盘申请一致；已有历史账本和决定不变；新数据截止日与下一官方交易日正确；重复处理同一日期复用完成结果；缺失日和未完成日拒绝入账。已有13项连续及月度训练测试和214全图增量核对直接复用。验证日期参数化可以使用保存历史作离线等价检验，但不得把该检验算成新增策略账户或新的独立收益。

先保存固定研究起点，不需要为保存起点再训练或生成账户。随后仅在新日期输入满足条件时运行预定续算。计划零新参数、零非月首拟合、22条既有研究账户增量续接、零新参考策略。新主评价记录只计算两档费用；来源账户用于真实依赖，不能按22个新策略累计。

结果同时呈现原固定策略全期指标、冻结后独立日期的完整净收益序列、交易数及持仓等待日。费用、242日年化、零现金利息、整百份、T+1、分红和所有进出场规则保持。冻结后的短段波动为零时夏普未定义，不把现金九天写成高夏普；样本不足时不宣称稳定达标。不根据短段结果切换209候选或重新选择起点。

历史目标目前已通过，独立稳定表现仍未建立。不得通过重复旧历史、重采样或回填未来记录补足独立证据。本项完成工程与起点保存属于实际进展；只有外部新数据确实缺失且没有其他必要实现可推进时，才按持续目标的连续阻塞规则处理。保持原持续任务，市场未产生新数据时安静，不新增任务或重复Word、ZIP、GPT审阅包。规则和新结果使用中文MD。
