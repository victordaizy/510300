# 第215轮：可变日期的固定策略续算入口

策略及历史指标沿用214。主方案仍按206来源85%和168来源15%合成，普通调仓门槛十个百分点、明确零全部退出；所有内部因素、进入、退出、模型锁定与费用细则见214交付。此次不添加因素或候选，不新增独立绩效样本。

## 入口行为

参数明确指定上一账户目录、上一因素文件、新因素文件、分红文件、原两条模型序列、新截止日期、输入接纳回执和隔离的新输出目录。旧正常账本直接拼接，22条账户仅计算官方日历中上一收盘之后的新增日期。

新输入必须覆盖每个新增官方交易日，末日已有完整收盘，分红覆盖不早于末日。未完成日、缺失日、重复日期或已接纳历史被改变时拒绝续算。月份首个交易日需提供原日程的模型记录，非月首不拟合；已持仓仍由原恢复引擎保留入场模型。

每个来源完成后保存恢复状态；中断后可复用完整来源。Windows实际文件锁保护同一目录，不把残留锁文件误当成运行进程。同一参数及输入重复提交已完成截止日，直接返回原保存结果，不再运行账户或覆盖文件。

决定表中的15:05是原规则模拟时钟，实际结果保存时间单独记录，并逐日判断是否早于下一官方开盘。迟到的历史补算不改写时间，也不能登记为及时的前瞻决定。工程验证模式与新增完整日线研究模式分别标记，均不代表真实成交或实际持仓。

## 本次一次性验证

先使用213的8月31日状态，仅接9月1日；再使用这份新状态接9月2日至9月11日八天。22个账户共44段、198条增量行，用于检验不同日期参数及连续两次恢复。与214已经保存的全部22条完整账本、决定、持仓周期及最终控制器状态逐值比较。不会重新训练、重放22条完整历史或改动214记录。

随后对第二段相同输入再调用一次，要求直接返回完成结果，保存文件数值及内容不变。六项针对性测试覆盖单日与多日、周末日历跳转、缺失日、未完成日、分红覆盖不足、必要月首记录及活进程锁释放；已有十三项连续引擎和训练测试复用。

当前研究延续起点已在2026年9月13日10:49:31保存，已知行情截止9月11日。该起点不重建。215只完成新增日期入口及等价检验，零新候选、零新拟合、零新主评价记录、零新增交易日。下一官方交易日为9月14日，未来研究仍要先取得并接纳完整新行情及分红覆盖。

历史净夏普及年化目标已经通过，稳定独立证据未建立。工程等价检验不能增加独立收益日期；保持原持续研究任务，无券商、订单、实盘、Paper或Shadow，无GPT包、ZIP或重复Word。
