# 第103轮之后：过去预测误差能否支持模型选择

第103轮未达标。主基础／压力夏普1.115／1.056，较早0.442／0.406；原91分别1.233／1.174和0.431／0.395。较早略升，主明显下降，两段年化收益均低于买入持有。关闭这一固定交互组合，不通过改窗口、交互或父策略救回。

103四测试3.76秒，一个新连续九项学习参考及四账户约7.59秒；第102轮114模型和原91急跌参考直接复用，没有新训练。主201持股收盘45成交、早340收盘35成交，所有目标完整，无未成交。159月首预算、参考成熟预测、早历史前缀及四账户均已核，不重跑本轮。

102单策略主0.575/0.511、早0.781/0.758；103组合把旧91主点候选拉低至1.115/1.056，早仅改善至0.442/0.406。不能事后按年份选102或91，也不能将单笔2019改善外推成泛化。原91仍仅主基础局部1.233，压力、较早与超额没有完成。

下一104先做有界的输入可用性核对：原八项和固定九项退出模型，能否在同一自然参考状态上，按不晚于该状态原点的历史模型生成两条过去预测，并在该自然周期完全退出后才观察两条误差。复用原1461个D60状态及两套保存模型，不重训、不重跑参考。只先检查同期预测对的完整周期数、成熟日期及最早可用日期，不先计算选择器或组合收益。

拟研究的机制为按已成熟预测误差选择或混合退出模型，不按年份、事后夏普或当前持仓未结束的盈亏选模型。它与已做的两个实际策略收益协方差预算、过去Sharpe选择器和第90轮市场条件预测父组合差额不同。但尚需仅针对已实现模型选择/预测误差组合做一次有界查重，然后才能登记一个有限规则。不要退回旧固定共享、HMM、持仓CUSUM或RBF参数搜索。

若有完整同期预测证据，可论证采用周期等权、最近20已成熟完整周期和原10周期100状态下限的非负最小均方误差混合。误差使用预测减相同已成熟继续价值标签；目标为未去均值的误差平方，明确包含偏差，不冒充收益方差。预测初期或差值退化时如何保留无观点及沿用基线须在新结果前明确。当前这仅是方向，不能把待选规则当已冻结或已改善。若完整同期周期不足直接放弃，不降低样本门槛或借未来模型。

唯一最新状态以总索引next_work为准；104尚无实现、配置、模型或账户。继续完整费用与全部现金日、中文规则、必要检查，暂停EPS等慢源和GPT包。

## 同期预测可用性已经完成

2026年9月8日18:22:05完成只读核对，见reports/research/510300_exit_forecast_pair_availability_20260908/result.json。1461个原状态中1011个同时有当时成熟的两模型；23个完整周期的964个状态全部同期可用，另一个周期只有部分状态可用。按最近20个完整同期周期、原十周期一百状态下限，共66个月度原点具有支持，最早为2021年3月1日。没有计算误差数值、选择权重或新账户。

因此这条机制无法在2015—2019年取得新校准权重，不能声称它能独自修复较早历史。若继续一个固定设置，早期须明确沿用原八项基线，模型本身未知时仍按原价格退出，不用未来模型或降低最低周期数。可先检验主阶段是否能在两种已有预测之间按真实过去误差适应，完整目标仍未实现。

有界查重只命中本方向文档、旧中位数训练损失说明及EPS误差归因，没有找到同一两套退出预测的成熟误差混合实现；不要重复宽泛检索。拟定单一解析规则：每个月首，以完整同期成熟周期等总权重计算两预测各自误差平方均值和误差乘积均值；旧模型权重为“新误差平方均值减两误差乘积均值”除“两误差之差的平方均值”，限制在零至一，新模型为一减旧模型。误差不去均值，这是最小均方预测误差，不是策略收益风险预算。每周期各状态平分该周期权重。窗口不足、必需值缺失或两误差完全相同无法识别新权重时，记录无新校准观点并沿用上次权重，初始旧模型为一、新模型为零。

每个持仓收盘，两套模型均须按当时真实持仓输入计算，使用当月已经确定的权重混合预测，仍连续两次负值才退出；非零权重所需预测未知则混合未知、负数计数清零、原价格时间退出继续。原进入、重现、冷却、费用、两段账户均不改。此规则目前为待登记设计，需新混合数学、同期模型时钟、完整周期选择和实际退出必要测试后才能冻结运行。零权重支路的未知预测不能阻断已知基线，但不能把未知误差填零加入校准。
