# 第152轮计算复用补充：保留完整月度时钟，只拟合新的输入组合

原方案与输入绑定不修改。2026年9月9日的保存输入核对已经完成：1461个原状态、141个月度记录，其中114个月有成熟模型，27个月仍无成熟模型；首次成熟为2017年3月1日。114个月统计的一阶残差延续系数范围为−0.05420142388845348至0.8479399692481128，中位数0.5927247101821119。没有触及预定负0.99至正0.99边界，没有相邻状态日历缺口。

这114个月只有25种不同的输入组合。身份由原保存模型的参数、标准化和周期截距，以及按原次序列明的训练周期和状态原点共同确定，已有原数据文件另有绑定。不同月份可能对应完全相同的成熟样本和原参数。月份和训练窗口相互重叠，这些统计不构成114次独立证据，也未证明误差修正会改善实际交易。

第152轮若实施，按原141个月度记录顺序推进。第一次遇到某输入身份时，用固定相关系数估计和固定广义最小二乘规则计算新退出模型。后续月份若输入身份完全相同，复用此前已经完成的该模型及其成功或失败状态，不重复拟合，也不在相同输入下重试失败结果。

每个月仍保存该月检查时点、成熟成员、输入身份、是否首次拟合、参数首次计算月份及模型身份；原27个不足月份仍无模型。实际新交易只可选进入日首个持仓收盘以前已可用的月度记录，模型参数复用不能引入后来月份。统计时分别报告141个月度检查、114个成熟模型记录、实际不同输入拟合次数及复用次数，不能把模型记录数写成新训练次数。

在当前来源保持一致且数值计算都成功的前提下，最多需要25次不同输入的退出模型拟合。该复用只减少重复计算，不改变模型、输入、权重、时点、预测、进出规则、费用或任何验收条件；它不是增加新的策略设置。

目前仅完成原残差统计和输入核对，新退出模型拟合、新退出预测、新账户均为零。下一步仍需必要数值测试和正式冻结，然后一次训练及完整资金账户运行。
