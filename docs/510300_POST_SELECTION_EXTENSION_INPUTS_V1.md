# 第212轮准备方案：接纳已核对的补充行情，建立来源续算所需的最小依赖图

本项在输入正式接纳前固定范围；已完成来源精度和旧因子一致性的只读预检。第211轮已用各一次请求取得新浪、腾讯和两个官方分红来源；两行情共同30日的开高低收完全一致，2026年8月17日至9月11日二十个新交易日均齐全，分红候选覆盖核对到9月11日且无新事件。原始响应、解析行情和回执已经保存，不重复请求。

## 固定范围

继续固定209的简单两来源方案：85%为206的目标1.15倍来源，15%为168的相加封顶来源，外层十个百分点门槛。保持所有内部原始因素、已有模型时点、参考周期、风险窗口、份额约束、费用及退出规则；零新候选、零新拟合、零新交易账户。本项只完成输入接纳、因子前缀一致性和最小依赖及状态恢复路线。

二十个新日期都发生在209配置2026年9月13日09:03:57正式冻结之前。它们未纳入本次308路径矩阵，可用于补充历史和工程接续检查，但不能称为严格前瞻。当前冻结后已经完成的市场交易日仍为零；下一官方日历交易日为9月14日。没有事先形成的日决定回执，就不能仅凭新增价格补出前瞻成绩。

## 输入接纳与不改旧历史

只读211目录保存的新浪和腾讯行情、原始响应与官方分红候选覆盖，不再上网。先对2026年8月3日至14日重叠日期核对现有原始与已修正价格；核对两新来源的成交量和成交额单位、非缺失性与数值差异，不能仅凭开高低收一致就忽略成交量字段。若有差异，完整列出并说明哪些因素会受影响，不默默修正或插值。

在本研究新目录建立“原已修正3456行＋20行补充行情”的候选输入，期望3476行。旧三条历史价格修正、原分红事件和日期顺序保持。所有旧输入文件和特征文件保持原样；候选分红覆盖只在新目录引用，不覆盖现有全局分红覆盖文件。

使用既有纯特征生成方法，在新目录生成延长后的日频因素，检查截至8月14日的原3456行数值因素是否与已冻结特征一致。每个旧日期只能使用当时及之前的数据；不能因为新日期出现而回改旧窗口、标记或数据有效性。只在旧因子前缀一致且新交易日与分红覆盖清楚时标为可用于下一项补充历史工程验证，不计算新策略收益。

## 最小来源依赖图与状态

只从简单方案两个非零来源出发：206的ADD_GATE_EXPOSURE_115，以及168的RUNS_OPPORTUNITY_CAPPED_SUM。根据各自冻结配置、实际输入路径和纯计算入口追到需要的原始特征、参考目标及已保存模型；零权重来源不因旧批次曾经一同运行而自动加入续算范围。不得宽泛重扫或重跑所有历史研究。

输出每个必要节点的来源标识、配置及程序路径、直接父节点、价格与分红输入、原模型文件或参考周期依赖、是否有可恢复状态、下一项应调用或适配的入口。明确区分策略算法不变、来源账户重放、原规则规定的月度模型更新和新增优化试验，不能悄悄改变训练时点。

211已经确认主压力简单账户在8月13日收盘持有61800份、正常8月14日申请为零，8月14日终点开盘却强制卖出61800份。这个结算动作不是自然退出。后续应从8月13日及之前的因果状态恢复，再按原规则正常处理8月14日开盘及收盘，才能走向8月17日以后的行情。不能从研究终点清算后的现金余额直接续算，也不能用新二十万元重置来源掩盖不连续。

外层现金、份额和应收分红虽可在账本读取，内部可能还需要持仓周期、退出等待、两次零计数、已实现收益窗口及已成熟模型版本。必须据必要程序具体盘点，不能仅因为有日账本就宣布已经有完整恢复快照。若需从更早日期重放，应明确最小必要范围和输出隔离目录，保留旧结果。

## 交付和下一步

交付新候选输入、旧因子前缀比较、补充数据覆盖表、最小依赖图及一个明确可执行的来源恢复方案。此项无新预测、账户或夏普；不启动券商、订单、Paper、Shadow或新的策略服务，不制作GPT数值包或重复Word。

后续实际续算需另行固定工程入口并验证旧正常区间重放一致，再用补充历史检查连续性；只有真正冻结之后、决定在执行前留存的新数据，才可能积累前瞻证据。当前已有的历史夏普和年化通过结果保持，独立稳定实现目标仍待证据。
