# 第216项：接上免费新增日线，收盘数据到齐才续算

215已完成通用日期入口 research/fixed_date_continuation_v1.py：22账户按1日加8日拆分，与214保存的38940条账本及完整状态精确一致；同一日期重复提交直接复用。六项针对性测试、一次实际两段检验和保存核对均完成，不重新运行215的prepare、run、verify或拆分检验。

当前实际研究延续源仍为 reports/research/510300_september_monthly_continuation_v1；215目录下first_day和remaining_days仅是工程检验，不是新的研究时间起点。原研究起点记录 reports/research/510300_fixed_research_origin_v1/origin.json 在9月13日10:49:31保存，绑定22条状态及两模型，下一官方交易日9月14日。

## 有效工程和外部等待分别处理

下一项先完成一个可接收日期和输出目录的免费输入适配器，直接复用211和212已经验证的来源、解析口径、价格精度及因素计算。入口把已接纳日期作为起点，用官方日历判断是否有更晚的完整收盘，15:05以前不接纳当日日线。同一已完成日期不再请求来源、不生成新轮次；只有实际新增收盘日才请求一次既有四来源。

脚本 scripts/post_selection_data_feasibility_20260913.py 的 fetch、parse_prices 与官方分红核对是既有实现参考，不执行其旧main、不修改其旧输出。新实现将输出目录、查询起止日显式传入。用已有211原始响应对解析输出做一次必要兼容检验，可复用212精度回执，不重跑整个212输入研究或215账户。

目前为2026年9月13日周日，最后完整交易日9月11日与已接纳日期相同。完成适配器后，只记录明确的“暂无新收盘日期”，不进行无意义网络请求；这不是正在运行的等待进程，也不等于新的前瞻样本。

## 一次获取、接纳并续接

当有新收盘日时，用新浪完整日线、腾讯日线交叉核对新增日期的开高低收，成交量及金额保留212的原单位与精度定义。每个来源保存实际请求和取得时间、原始响应及内容身份。失败保留真实缺项，不改用付费来源、不关闭TLS、不重复请求已经完整保存且日期相同的成功响应。

分红沿用基金管理人官网与上交所公告，覆盖需达到新增截止日。已登记十四事件无变化时不改旧事件；若出现新事件，必须取得官方登记、除息、支付日期和每份金额后才能追加独立候选文件，不能直接把“未核对”当成“无分红”。旧全局输入、原价格修正和历史因子不覆盖。

只把旧接纳价格之后的完整新行追加到独立文件，调用原 research/adaptive_allocation_v1.py 的 factors，旧因素前缀需一致。给215入口生成实际接纳回执：接受状态、最后日期、完整取得时间、分红覆盖日期、因素及分红文件内容身份、新日线证据类别；另附原始来源和价格核对结果的定位。不得手工把失败状态写成通过。

生成通用续算设置，指定214或其后最新已完成的正式研究目录、旧因素、新因素、当前两模型、实际接纳回执、新截止日和新输出目录。原两来源权重、阈值、费用、日历、进入退出和模型周期均不变化。非月首零拟合；月首缺原定模型记录时先按既有规则补模型，不能跳过日程。

首个新增开盘继承旧已保存申请。分开记录规则模拟时钟与实际计算保存时间；新增数据、过去决定和研究模拟成交各有自己的时间，不据文件名回填。晚于下一开盘生成的决定保留迟到状态，不能算及时前瞻。主策略与内部来源的现金或持仓只是研究状态，仍不连接券商或启用实盘、Paper、Shadow。

## 输出与停止条件

本项新增工程不算策略候选或新主评价。只有确实进入新日线并完成两费用主账户后，才增加新绩效记录；来源账户只是依赖，不按22套策略累计。报告固定方案全期指标与冻结后新增日数、净收益、交易和等待日；波动为零或样本不足时不宣称夏普稳定达标。

完成适配、必要兼容验证及无新日期行为后，不再以重复旧工作维持进展。新完整交易日和真实取得结果是剩余外部条件；若同一条件连续三个目标回合都构成实际僵局，按目标阻塞规则处理，保留原持续任务在下一有意义时间检查。原任务通知偏好保留，无变化安静；不新增任务、Word、ZIP、GPT审阅包或安全审计。
