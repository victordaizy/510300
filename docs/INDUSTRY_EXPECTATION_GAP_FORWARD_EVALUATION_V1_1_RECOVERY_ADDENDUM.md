# 行业预期差前瞻评价 V1.1 冻结输入恢复附录

状态：`RECOVERY_ADDENDUM_AFTER_FIRST_FORWARD_OBSERVATION`

本附录不改变 `INDUSTRY_EXPECTATION_GAP_FORWARD_EVALUATION_V1` 的预测、行业方向、权重、入场日、60/120 日期限、成熟日、评分规则、门槛或停止条件。它只修复一个验证层缺陷：原预测清单把会随日常 PCF/IOPV 采集更新的运行就绪报告登记成了冻结路径。

## 事故事实

- 原预测清单自身 SHA-256 保持 `dc0070434375b032f2c316db0b12cf026e042ed7647d5924b6cad368ccbe3e09`。
- 17 个冻结对象中 16 个仍逐字节匹配。
- 唯一漂移路径是 `reports/data_quality/510300_primary_market_readiness.json`；它在 2026-08-19 正常采集后更新，不能再代表 2026-08-18 冻结原件。
- 2026-08-18 采集日志末尾保留了当时完整 JSON。按原 Windows `CRLF` 序列化后为 967 字节，SHA-256 恰为原清单登记的 `1d4432ce581cf63f449315ef4932230fccc7e15a09affcac9e2662121fa5d2d8`。
- 恢复原件中的 4 个观察日、2 个完整日、20/40 日门槛及两个禁用授权字段，与冻结 ETF 快照内嵌语义完全一致。

## 允许的执行方式

V1.1 每次运行必须同时验证：

1. 原 V1 评价清单及其全部冻结文件；
2. 原预测清单本身哈希；
3. 除已声明路径外的所有原预测冻结文件；
4. 恢复副本的字节数、SHA-256、来源日志哈希和确定性重建结果；
5. 恢复副本与冻结 ETF 快照的关键语义；
6. 原预测结果和行业预测账本的显式哈希；
7. V1.1 自身恢复清单。

当前运行就绪报告继续保留在原路径供 PCF/IOPV 日常监控使用。恢复副本只用于证明 2026-08-18 冻结身份，严禁覆盖当前报告，也不得成为新的预测输入。

## 继续有效的禁止项

- 未成熟前不输出部分收益、浮盈亏或临时命中率；
- 不改写原 `NO_VIEW`；
- 不把同日 25 个行业当作 25 个独立时间样本；
- 不生成仓位、订单，不连接券商，不启用实盘；
- 任何未声明漂移、恢复哈希失败或语义不一致均为 `INVALID` 并停止评价。
