# 解禁公告来源 V1.1：修复分页覆盖，保留 V1 失败

V1 已在 2026-09-14 00:05:10 完成140个月采集，33,722条返回行中有186个重复公告编号。全部重复都发生在相邻分页，同一证券、同一时间、同一PDF；80个月唯一编号数量不足，原状态 `NO_VIEW_INCOMPLETE_PRIMARY_ANNOUNCEMENT_COVERAGE` 永久保留。不能删除重复后把少掉的记录当作零，也不能将原回执改成通过。

免费接口实测会把 pageSize=1000 仍限制为30条；短日期查询可以一次取得完整小集合，2020-01-14 的63条命中也可由沪市47条与深市16条核对。V1.1只改变请求分区方法，不改变2015-01-01至2026-08-14、搜索词、标题资格规则、成员名单及未来研究用途。此时尚未生成新收益标签、模型或账户，因此这是一项有原始证据的来源修复，不是对失败收益试验的重新设计。

保留并复用V1中60个完整月份的原始响应。仅对80个失败月份做自然日区间二分，直到一次响应容纳整个区间；每次子区间总量之和必须等于父区间原声明量。同一天超过30条时用沪、深、北板块拆分，三者总量和及编号集合必须与父区间相符；若单日单板块仍超过30条，最多完整分页三遍，仅当去重集合恰好等于始终不变的声明总量、相同编号的证券/时间/标题/PDF完全一致时通过。每月最多160次HTTP尝试，每页最多2次，3个月并发。不能无限重试。

每个原始响应、请求参数、收发时间、哈希、失败尝试及分区判定单独保存。二分区间必须不相交且连续覆盖请求日期；板块分区不能重叠。缺页、来源变更、日期越界、编号冲突或预算耗尽都保持 `NO_VIEW`。未通过时没有模型、收益或策略达标结论。

完成后另存原V1与V1.1新增/缺少编号差异，不覆盖V1的33,722条返回行。来源通过只表示当前历史档案查询的覆盖一致，不能证明过去逐次HTTP首发时间。历史发布日期如用于模型，必须在另外冻结的模型协议中加明确的可用时间假设。
